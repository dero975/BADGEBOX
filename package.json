{
  "name": "my-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "export-zip": "npm run build && cd dist && zip -r ../BADGEBOX.zip . && cd ..",
    "upgrade-github": "node upgrade-github.js",
    "upgrade-safe": "chmod +x upgrade.sh && ./upgrade.sh"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
