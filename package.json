{
  "name": "my-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "export-zip": "npm run build && cd dist && zip -r ../BADGEBOX.zip . && cd .."
  },
  "dependencies": {
    "vite": "^5.4.8"
  }
}
